---
all:
  vars:
    # General deployment for all hosts
    deploy_node_exporter: true
    deploy_collectd: true
    deploy_pcm: true

# Server Hosts
grafana-server:
  hosts:
    am-grafana.work.home:

prometheus-server:
  hosts:
    am-prometheus.work.home:

prometheus-alertmanager:
  hosts:
    am-alertmanager.work.home:

loki-server:
  hosts:
    am-loki.work.home:   


# Systems to be monitored
monitoring-agents:
  hosts:
    am-agent.work.home:
      deploy_pcm: false
      deploy_node_exporter: false 
    am-agent02.work.home:
      deploy_collectd: false
 